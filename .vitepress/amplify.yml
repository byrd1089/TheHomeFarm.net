version: 1
frontend:
  phases:
    build:
      commands:
        - npm run docs:build
  artifacts:
    outputDirectory: docs/.vitepress/dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*