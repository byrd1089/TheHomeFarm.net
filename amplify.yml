version: 1
frontend:
  phases:
    build:
      commands:
        - npm run docs:build
  artifacts:
    baseDirectory: docs/.vitepress/dist
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*